import styled from 'styled-components'

export const StyledList = styled.ul`
 list-style-type: none;
 margin: 0;
 padding: 0;
 width: auto;
 height: inherit;
 display: grid;
 overflow-y: auto;
 direction:rtl; 
`